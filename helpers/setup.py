from setuptools import setup, find_packages

setup(
    name='python-helpers',
    version='0.0.1',
    description=("Useful python functionality that is not built-in."),
    packages=find_packages()
)
